{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Run guess.main",
            "type": "debugpy",
            "request": "launch",
            "module": "guess.main",
            "cwd": "${workspaceFolder}",
            "env": {
                "PYTHONPATH": "${workspaceFolder}"
            }
        }
    ]
}