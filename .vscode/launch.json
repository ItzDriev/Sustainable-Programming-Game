{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Run PigGame.main",
            "type": "debugpy",
            "request": "launch",
            "module": "PigGame.main",
            "cwd": "${workspaceFolder}",
            "env": {
                "PYTHONPATH": "${workspaceFolder}"
            }
        }
    ]
}